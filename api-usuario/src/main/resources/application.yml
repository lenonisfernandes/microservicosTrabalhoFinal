spring:
  datasource:
    url: jdbc:postgresql://pos-db.cqryu1enxyeo.us-east-1.rds.amazonaws.com:5432/notas
    username: postgres
    password: 26612430

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQL81Dialect

  application:
    name: USUARIO

  mvc:
      pathmatch:
            matching-strategy: ANT_PATH_MATCHER
            
server:
  port: 8081

eureka: 
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
      register-with-eureka: true
      fetch-registry: true
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance-id:${random.value}}
